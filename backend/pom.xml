<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.3</version>
        <relativePath/>
    </parent>

    <groupId>com.edgeplan</groupId>
    <artifactId>edgeplan-backend</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <name>MND EdgePlan-AI Backend</name>
    <description>MND EdgePlan-AI Enterprise Production Planning &amp; Optimization Service</description>

    <properties>
        <java.version>17</java.version>
        <ortools.version>9.10.4067</ortools.version>
    </properties>

    <dependencies>
        <!-- Spring Boot Web -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Validation -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Google OR-Tools Java API (Targeted for Windows x86_64) -->
        <dependency>
            <groupId>com.google.ortools</groupId>
            <artifactId>ortools-java</artifactId>
            <version>${ortools.version}</version>
            <exclusions>
                <exclusion>
                    <groupId>com.google.ortools</groupId>
                    <artifactId>ortools-darwin-x86-64</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>com.google.ortools</groupId>
                    <artifactId>ortools-darwin-aarch64</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>com.google.ortools</groupId>
                    <artifactId>ortools-linux-x86-64</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>com.google.ortools</groupId>
                    <artifactId>ortools-linux-aarch64</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>com.google.ortools</groupId>
            <artifactId>ortools-win32-x86-64</artifactId>
            <version>${ortools.version}</version>
        </dependency>

        <!-- DevTools for quick restart -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-devtools</artifactId>
            <scope>runtime</scope>
            <optional>true</optional>
        </dependency>

        <!-- Test dependencies -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
